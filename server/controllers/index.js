'use strict';

/**
 * Paper Trail controller file
 * This registers the controller functions to be used by the routes
 */
const paperTrailController = require('./paper-trail');

module.exports = {
  paperTrail: paperTrailController,
};